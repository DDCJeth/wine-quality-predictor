version: 2.0
jobs:

  unittests:
    working_directory: ~/repo
    docker:
      - image: python:3.7
    steps:
      - checkout
      - run:
          name: Install test dependencies
          command:  pip install -r tests-requirements.txt # PUT YOUR BASH COMMAND HERE TO INSTALL THE TEST DEPENDENCIES
      - run:
          name: Run unit tests
          command: pytest tests/*.py  # PUT YOUR COMMAND HERE TO RUN THE UNIT TESTS


workflows:
  version: 2
  test-and-deploy:
    jobs:
      - unittests:
          filters:
            branches:
              ignore:
                - master
